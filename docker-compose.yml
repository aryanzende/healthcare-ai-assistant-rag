version: "3.9"

services:
  api:
    build: .
    container_name: healthcare-ai-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./vector_store:/app/vector_store
